# 安全策略

## 支持范围

`action-guard` 当前仍处于 `early preview` 阶段，因此这里的安全支持策略以“快速确认问题边界”和“避免公开泄漏敏感细节”为主。

当前更关注以下类型的问题：

- 默认配置导致的敏感信息暴露风险
- 治理接口或示例接口中的越权、误用或危险默认行为
- 组件主链路中的反序列化、脚本执行、任意外部调用等高风险缺陷
- 公开文档、示例或模板中不安全的推荐配置

## 如何报告漏洞

如果你发现的是潜在安全问题，请不要直接公开提交 issue。

建议至少提供以下信息：

- 影响范围
- 复现条件
- 触发步骤
- 可能的安全影响
- 如果已有临时缓解方式，也请一并说明

当前仓库还没有专门的私密安全报告入口，因此在正式建立安全联络渠道前，建议先不要公开披露可直接利用的细节、payload 或攻击路径。

如果问题尚不确定是否属于安全漏洞，也可以先按最小信息量描述风险边界，等维护方确认后再决定公开方式。

## 披露预期

在 `early preview` 阶段，维护策略是：

- 优先确认问题是否成立
- 优先确认是否影响默认主链路或公开示例
- 修复或缓解方案准备好之前，尽量避免公开利用细节

## 当前不承诺的事项

当前不承诺：

- 已建立正式 CVE 流程
- 已建立长期版本分支安全回溯支持
- 对所有非主链路模块提供同等级安全响应

因此更准确的期望是：

- 主链路风险会优先处理
- 非主链路或占位模块按实际影响评估
