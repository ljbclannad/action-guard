# action-guard 协作约定

本文件适用于整个仓库。开展工作时遵守以下项目约束，详细设计按任务类型阅读对应文档。

## 当前阶段与演进原则

项目处于持续完善的早期阶段，模块划分、内部接口和实现方式可以随已确认的需求调整。以下可靠性要求和操作授权规则持续适用；标注为当前实现的类名、能力状态和字段行为是工作入口，不是永久设计承诺。

- 优先形成可运行、可验证的完整主链路。为当前问题选择足够简单的实现，扩展点应有实际用例支撑。
- 在本次任务范围内，可以合并重复实现、调整内部接口和改进模块边界；说明调整原因及影响，同步修改调用方、测试和相关文档。
- 不为假设中的外部调用方预先增加兼容构造器、适配层或废弃流程。涉及公共 API、SPI、YAML 或持久化格式时，检查已有使用、发布承诺和存量数据，再决定直接调整还是提供迁移方案；不能仅凭项目尚早就假定没有使用者。
- 已知缺陷、占位能力和临时实现应随需求完善。保持改动聚焦，不为保留当前实现而绕开问题，也不把局部任务扩大为整仓重构。

## 输出语言与命令安全

- 与用户沟通，以及文档、注释、测试说明、审查结论等可读产物，均使用简体中文。代码标识符、命令、路径、配置键和标准协议名保留原始语言。
- 进行代码编写或修改时，不执行任何 Git 命令或 Git 操作；仅在用户明确提出 Git 相关请求后才可执行。
- 提交、推送、创建 issue、发布或删除文件前，取得用户明确授权。
- 执行命令前确认精确目标，优先只读、可逆、影响范围明确的操作。需要递归、覆盖、强制、提升权限或影响外部系统的命令时，先取得用户明确授权。
- 保留已有用户修改。修改前阅读相关实现、约定和测试，采用最小可验证改动，避免无关重构。

## 项目定位与模块职责

action-guard 面向 Spring Boot 应用，为主交易提交后的异步副作用提供 Outbox 发布、串行步骤编排、重试、补偿和人工治理能力。扩展到 DAG、并行分支或复杂 DSL 前，先确认目标、边界、验收标准和非目标。

下表用于定位当前实现。调整归属时保留清晰的职责边界，并同步更新装配、依赖和受影响文档。

| 模块 | 责任边界 |
| --- | --- |
| `action-guard-api` | 公共请求、定义、执行上下文与 SPI 契约 |
| `action-guard-core` | 发布与执行编排、状态迁移、恢复、补偿；通过接口访问存储和消息通道 |
| `action-guard-spring-boot-starter` | Spring 自动装配、配置、事务接入和调度触发 |
| `action-guard-store-mysql` | 持久化、映射和乐观锁，不承担步骤编排 |
| `action-guard-adapter-rabbitmq` | 消息转换、发送、消费去重与 ACK 决策，不实现业务 Handler 逻辑 |
| 能力层的 IM、Notify 等适配器 | 将步骤上下文转换为能力请求，通过 Sender 接口连接厂商实现 |
| `action-guard-ops-api` / `action-guard-ops-web` | 人工治理、查询、审计，以及独立应用入口 |

使用非主路径模块前，核对实际实现与 [模块选择建议](docs/module-selection.md)，区分已实现能力与规划。评估内存实现是否需要保留时，检查其测试和演示用途。

## 核心执行约束

- 发布 Action 时，业务更新与 Action、Step、Outbox 的创建应在同一本地事务内提交；首次 MQ 投递在事务提交后触发。
- 首次发布、步骤推进和恢复扫描共用单条 Outbox 投递逻辑，当前入口是 `ActionOutboxDispatcher`。允许重命名或重组实现，但保持抢占、发送和状态回退规则统一。
- 发送前通过记录版本抢占 `CLAIMED`；失败回退 `NEW`，成功保存 `DONE`。乐观锁冲突时退出，不能用旧快照覆盖其他执行者的新状态。
- 只有恢复扫描确认超时后，才可重新接管 `CLAIMED` 记录。尚未到期的任务留给后续扫描。
- 传输失败后的重发保留 `dispatchId`；推进下一步骤或调度业务重试时生成新的 `dispatchId`。
- `DONE` 表示消息发送成功且发布状态已落库，不代表消息消费完成或整个 Action 成功。MQ 与数据库之间仍存在重复投递窗口，消费端与业务 Handler 必须考虑幂等。
- Action 状态迁移规则集中维护，当前通过状态机与 `ActionTransitionService` 实现。调整时检查 Step、Action、迁移日志和后续 Outbox 的一致性；不能仅凭已有乐观锁推断跨表操作具有事务原子性。
- 修改计数、告警或展示前，核对字段的全部写入与读取位置。发现投递重试与业务重试等语义混用时，可以在任务范围内澄清或拆分，同时处理存量数据影响并更新文档。

## 实施与 Java 约定

- 对复杂需求先明确目标、边界、验收标准和非目标；信息不足时说明假设，避免把设计建议直接扩大为实施范围。
- 修复缺陷前建立可复现证据，并补充能够验证故障场景的回归测试。
- 公共接口、构造器、DTO 和配置发生变动时，同时搜索生产代码、测试、示例与文档中的引用，并说明兼容性影响。
- 调用 Java 标准库工具类时显式导入类型，例如导入 `java.util.Collections` 后使用 `Collections.emptyList()`，不在业务代码中使用全限定名调用。
- 优先使用现有依赖注入和接口边界；已有结构阻碍明确需求时可以调整。提取公共逻辑应减少实际重复或集中关键规则，避免为少量相似代码增加新的模块或继承体系。

## 验证与完成标准

- 从受影响模块的定向测试开始；仅在风险、依赖关系或失败结果要求时扩大范围。
- 在仓库根目录执行定向测试，替换模块路径和测试类名：

  ```bash
  mvn -pl <模块相对路径> -am -Dtest=<测试类名> -Dsurefire.failIfNoSpecifiedTests=false test
  ```

- `failIfNoSpecifiedTests=false` 用于允许上游模块没有指定测试；仍需核对目标测试实际运行，不能仅凭 `BUILD SUCCESS` 判断测试已覆盖改动。
- 修改 Outbox 投递时，至少覆盖受影响的成功、失败回退、版本冲突和恢复场景；同时检查首次发布、步骤推进与扫描入口。按当前实现定位相关测试，测试保护行为契约，不固定内部类名、方法拆分或对象组装方式。
- MySQL 集成测试可能因缺少连接配置而跳过。核对测试报告中的运行数、失败数和跳过数；内存测试或 H2 测试不能替代真实 MySQL 验证。
- 运行 demo 冒烟或稳定性脚本前，先阅读脚本，确认数据库、RabbitMQ、端口及数据写入范围符合本次授权。
- 完成后说明改动结果、影响范围、实际验证和未验证项。外部可见行为、接口或重要配置变化时，同步更新相关文档。

## 按任务阅读文档

| 任务 | 阅读入口 |
| --- | --- |
| 调整模块归属、依赖或执行链路 | [模块架构](docs/module-architecture.md)、[架构设计](docs/architecture.md) |
| 修改 YAML 加载、定义模型或校验 | [定义规范](docs/definition-spec.md)，并核对加载器实际支持的字段，避免将设计字段当成已实现能力 |
| 修改 Starter 配置或接入方式 | [Starter 配置](docs/starter-config.md)、[快速开始](docs/quick-start.md) |
| 修改表结构、Repository、计数或状态持久化 | [数据模型](docs/data-model.md)，并核对当前 SQL 和映射实现 |
| 新增 StepType、Handler 或厂商适配 | [StepType 扩展指南](docs/step-type-extension-guide.md) |
| 修改人工重试、取消、跳过或补偿 | [治理操作](docs/ops-governance.md) |
| 修改指标、告警或审计输出 | [可观测性说明](docs/observability.md) |
| 修改公共契约或执行已授权的发布任务 | [兼容性与版本](docs/compatibility-and-versioning.md)、[发布纪律](docs/release-discipline.md) |

文档与实现不一致时，明确指出差异；描述当前行为以代码和验证结果为依据，期望行为以本次确认的需求为依据。调整上述边界或入口时同步维护本文件，替换失效规则，避免不断追加历史约束。历史执行结果、临时任务清单和待修复问题保留在相应交付说明或项目文档中。
